# Remote agent bundles

Release packaging downloads CI-built `ctl-agent-bundle-<version>-<target>.tar.gz`
archives and their `.sha256` files into this directory before invoking Tauri.
Development builds intentionally contain no placeholder executable bundles.
